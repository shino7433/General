# CW評価ループ（フェーズ2）実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 稼働中の「案件管理」シートの未判定案件を、ローカルのClaudeが日次で評価（Go/No-Go・見積・提案ドラフト）し、GAS Web App API経由で書き戻し、出先からHTMLダッシュボードで確認できるようにする。

**Architecture:** 壊れやすいシートI/Oは既存GASプロジェクトに追加する **Web App API（doGet/doPost・トークン保護）** に集約し決定的コード＋テストで固める。案件ページの読み取り・判定・ドラフトは **Claudeのエージェント手順（RUNBOOK）** が担う。純粋ロジック（pendingフィルタ・列マッピング・見積算出）は `node --test` でTDD。

**Tech Stack:** Google Apps Script（Web App: ContentService/HtmlService/PropertiesService、既存の GmailApp/SpreadsheetApp）、素のJavaScript（Node v18+ `node:test`、依存なし）、`curl`（ローカルAPIクライアント）、claude-in-chrome（案件ページ閲覧）。

## Global Constraints

- 設計書 [2026-07-07-cw-評価ループ-design.md](../specs/2026-07-07-cw-評価ループ-design.md) に完全準拠。
- CW上で**応募・送信・メッセージを一切行わない**。ループは案件ページ**閲覧のみ**、書き込み先は**自分のシート/リポジトリのみ**。送信は必ず人間。
- 列は [main.js](../../tools/cw-pipeline/main.js) の `HEADERS` と一致（1-based列番号: A=1 案件ID … E=5 カテゴリ, F=6 予算, G=7 報酬形態, H=8 URL, I=9 ソース種別, J=10 ステータス, K=11 判定, L=12 判定理由, M=13 提案数, N=14 見積, O=15 想定実稼働, P=16 提案ドラフト, Q=17 応募日, R=18 結果メモ）。
- `J`（ステータス）は未判定→`Go`/`NoGo` に進めるpendingゲート。`K`（判定）は評価verdictの凍結記録。判定値はJとKの両方に書く。
- pendingは `J === '未判定'` の行のみ。評価後はJが変わり再処理されない（冪等）。
- `api-shape.js` / `estimate.js` はGASとNode両方で動くよう末尾にNode用エクスポートシム（`if (typeof module !== 'undefined') module.exports = {...}`）。GASでは無視される。
- リポジトリ上は `.js`。GASへは既存の1ファイル構成（`コード.gs`）に**追記**する（全 `.gs` がグローバルスコープ共有）。
- **トークンは秘匿**。`CW_API_TOKEN` は `ScriptProperties` に保存し、ローカルは gitignore したファイル/環境変数で保持。**リポジトリにコミットしない**。
- コミットは日本語で簡潔に。作業ブランチ `claude/crowdworks-profile-setup-2gor9g` にのみコミット＆プッシュ。

---

## ファイル構成

| ファイル | 役割 |
|---|---|
| `tools/cw-pipeline/evaluate/api-shape.js` | 純粋ロジック（`filterPending` / `findRowByJobId` / `evalToRowUpdates`）。GASへ追記 |
| `tools/cw-pipeline/evaluate/api-shape.test.js` | 上のNodeユニットテスト |
| `tools/cw-pipeline/evaluate/estimate.js` | 見積・想定実稼働の算出（`estimate`）。GASへ追記 |
| `tools/cw-pipeline/evaluate/estimate.test.js` | 上のNodeユニットテスト |
| `tools/cw-pipeline/api.js` | GAS Web App（`doGet`/`doPost`/トークン/`buildSummaryHtml`/`initApiToken`）。GASへ追記 |
| `tools/cw-pipeline/evaluate/RUNBOOK.md` | Claudeが日次で辿る手順書 |
| `tools/cw-pipeline/README.md` | Web Appデプロイ・トークン設定手順を追記 |

依存の向き: `api.js`（GAS）は `api-shape.js` / `estimate.js` / `main.js`（`HEADERS`,`SHEET_NAME`）のグローバル関数を呼ぶ。テストは純粋関数のみ対象。

---

### Task 1: pendingフィルタ `filterPending`

**Files:**
- Create: `tools/cw-pipeline/evaluate/api-shape.js`
- Test: `tools/cw-pipeline/evaluate/api-shape.test.js`

**Interfaces:**
- Produces: `filterPending(rows: any[][]) → Array<{jobId,url,title,sourceType,row}>` — `rows` はヘッダーを除くシートの2次元配列。`row[9]==='未判定'` の行だけを、`row`（1-based シート行番号 = index+2）付きで返す。

- [ ] **Step 1: 失敗するテストを書く**

`tools/cw-pipeline/evaluate/api-shape.test.js`:

```js
const { test } = require('node:test');
const assert = require('node:assert');
const { filterPending } = require('./api-shape.js');

test('filterPending: 未判定行のみを{jobId,url,title,sourceType,row}で返す', () => {
  const rows = [
    ['111','2026-07-07','','A案件','','','','https://crowdworks.jp/public/jobs/111','スカウト','未判定'],
    ['222','2026-07-07','','B案件','','','','https://crowdworks.jp/public/jobs/222','スカウト','Go'],
    ['333','2026-07-07','','C案件','','','','https://crowdworks.jp/public/jobs/333','おすすめ','未判定'],
  ];
  assert.deepStrictEqual(filterPending(rows), [
    { jobId:'111', url:'https://crowdworks.jp/public/jobs/111', title:'A案件', sourceType:'スカウト', row:2 },
    { jobId:'333', url:'https://crowdworks.jp/public/jobs/333', title:'C案件', sourceType:'おすすめ', row:4 },
  ]);
});

test('filterPending: 未判定なしは空配列', () => {
  assert.deepStrictEqual(filterPending([['1','','','','','','','','','Go']]), []);
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `cd tools/cw-pipeline/evaluate && node --test`
Expected: FAIL（`Cannot find module './api-shape.js'`）

- [ ] **Step 3: 最小実装**

`tools/cw-pipeline/evaluate/api-shape.js`:

```js
function filterPending(rows) {
  var out = [];
  (rows || []).forEach(function (r, i) {
    if (String(r[9]) === '未判定') {
      out.push({
        jobId: String(r[0]),
        url: String(r[7]),
        title: String(r[3]),
        sourceType: String(r[8]),
        row: i + 2,
      });
    }
  });
  return out;
}

if (typeof module !== 'undefined') {
  module.exports = { filterPending: filterPending };
}
```

- [ ] **Step 4: テストを実行して成功を確認**

Run: `cd tools/cw-pipeline/evaluate && node --test`
Expected: PASS（2 tests）

- [ ] **Step 5: コミット**

```bash
git add tools/cw-pipeline/evaluate/api-shape.js tools/cw-pipeline/evaluate/api-shape.test.js
git commit -m "CW評価ループ: pendingフィルタ filterPending を追加"
```

---

### Task 2: 行検索と列マッピング `findRowByJobId` / `evalToRowUpdates`

**Files:**
- Modify: `tools/cw-pipeline/evaluate/api-shape.js`
- Test: `tools/cw-pipeline/evaluate/api-shape.test.js`

**Interfaces:**
- Produces:
  - `findRowByJobId(idColumn: string[], jobId: string|number) → number` — 列A（案件ID）の値配列から `jobId` に一致する **0-based インデックス**を返す。なければ `-1`。比較は文字列。
  - `evalToRowUpdates(ev: object) → Array<{col:number, value:any}>` — `ev` の提供キーのみを1-based列番号にマッピングして返す。キー→列: カテゴリ=5, 予算=6, 報酬形態=7, 判定理由=12, 提案数=13, 見積=14, 想定実稼働=15, 提案ドラフト=16, 結果メモ=18。`ev.判定` は **J(10) と K(11) の両方**に積む（列マッピングの後に追加）。

- [ ] **Step 1: 失敗するテストを書く**

`api-shape.test.js` に追記:

```js
const { findRowByJobId, evalToRowUpdates } = require('./api-shape.js');

test('findRowByJobId: 一致インデックス(0-based)/なければ-1/数値も文字列一致', () => {
  assert.strictEqual(findRowByJobId(['111','222','333'], '222'), 1);
  assert.strictEqual(findRowByJobId(['111','222'], '999'), -1);
  assert.strictEqual(findRowByJobId(['111'], 111), 0);
});

test('evalToRowUpdates: 提供キーを列マッピングし、判定はJ(10)とK(11)両方に積む', () => {
  const ev = { 判定:'Go', 判定理由:'要件明確', 提案数:3, 見積:'1万〜2万円', 想定実稼働:'2〜4h', 提案ドラフト:'本文', カテゴリ:'業務システム', 予算:'1万〜3万', 報酬形態:'固定' };
  assert.deepStrictEqual(evalToRowUpdates(ev), [
    { col:5, value:'業務システム' },
    { col:6, value:'1万〜3万' },
    { col:7, value:'固定' },
    { col:12, value:'要件明確' },
    { col:13, value:3 },
    { col:14, value:'1万〜2万円' },
    { col:15, value:'2〜4h' },
    { col:16, value:'本文' },
    { col:10, value:'Go' },
    { col:11, value:'Go' },
  ]);
});

test('evalToRowUpdates: 未提供キーはスキップ', () => {
  assert.deepStrictEqual(evalToRowUpdates({ 判定:'NoGo', 判定理由:'提案多数' }), [
    { col:12, value:'提案多数' },
    { col:10, value:'NoGo' },
    { col:11, value:'NoGo' },
  ]);
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `cd tools/cw-pipeline/evaluate && node --test`
Expected: FAIL（`findRowByJobId is not a function`）

- [ ] **Step 3: 最小実装**

`api-shape.js` の `if (typeof module ...` の前に追記:

```js
function findRowByJobId(idColumn, jobId) {
  var target = String(jobId);
  for (var i = 0; i < (idColumn || []).length; i++) {
    if (String(idColumn[i]) === target) return i;
  }
  return -1;
}

function evalToRowUpdates(ev) {
  ev = ev || {};
  var map = {
    'カテゴリ': 5, '予算': 6, '報酬形態': 7,
    '判定理由': 12, '提案数': 13, '見積': 14, '想定実稼働': 15,
    '提案ドラフト': 16, '結果メモ': 18,
  };
  var updates = [];
  Object.keys(map).forEach(function (k) {
    if (ev[k] !== undefined && ev[k] !== null) updates.push({ col: map[k], value: ev[k] });
  });
  if (ev['判定'] !== undefined && ev['判定'] !== null) {
    updates.push({ col: 10, value: ev['判定'] });
    updates.push({ col: 11, value: ev['判定'] });
  }
  return updates;
}
```

エクスポート行を更新:

```js
if (typeof module !== 'undefined') {
  module.exports = { filterPending: filterPending, findRowByJobId: findRowByJobId, evalToRowUpdates: evalToRowUpdates };
}
```

- [ ] **Step 4: テストを実行して成功を確認**

Run: `cd tools/cw-pipeline/evaluate && node --test`
Expected: PASS（全テスト緑）

- [ ] **Step 5: コミット**

```bash
git add tools/cw-pipeline/evaluate/api-shape.js tools/cw-pipeline/evaluate/api-shape.test.js
git commit -m "CW評価ループ: 行検索findRowByJobIdと列マッピングevalToRowUpdatesを追加"
```

---

### Task 3: 見積算出 `estimate`

**Files:**
- Create: `tools/cw-pipeline/evaluate/estimate.js`
- Test: `tools/cw-pipeline/evaluate/estimate.test.js`

**Interfaces:**
- Produces: `estimate(opts: {evalCount?:number, size?:'small'|'medium'|'large'}) → {見積:string, 想定実稼働:string}` — [crowdworks.md](../../crowdworks.md) §5段階（評価数 0〜2=low, 3〜9=mid, 10+=high）と §5/§7単価表から見積レンジを、案件規模から想定実稼働を返す。既定は `size:'small'`, `evalCount:0`。

- [ ] **Step 1: 失敗するテストを書く**

`tools/cw-pipeline/evaluate/estimate.test.js`:

```js
const { test } = require('node:test');
const assert = require('node:assert');
const { estimate } = require('./estimate.js');

test('estimate: 評価0件(low)×small は下限レンジ・2〜4h', () => {
  assert.deepStrictEqual(estimate({ evalCount:0, size:'small' }), { 見積:'5,000〜10,000円', 想定実稼働:'2〜4h' });
});
test('estimate: 評価5件(mid)×medium は中央レンジ・4〜8h', () => {
  assert.deepStrictEqual(estimate({ evalCount:5, size:'medium' }), { 見積:'20,000〜40,000円', 想定実稼働:'4〜8h' });
});
test('estimate: 評価12件(high)×large', () => {
  assert.deepStrictEqual(estimate({ evalCount:12, size:'large' }), { 見積:'50,000〜80,000円', 想定実稼働:'8〜16h' });
});
test('estimate: 既定は small・評価0(low)', () => {
  assert.deepStrictEqual(estimate({}), { 見積:'5,000〜10,000円', 想定実稼働:'2〜4h' });
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `cd tools/cw-pipeline/evaluate && node --test`
Expected: FAIL（`Cannot find module './estimate.js'`）

- [ ] **Step 3: 最小実装**

`tools/cw-pipeline/evaluate/estimate.js`:

```js
function estimate(opts) {
  opts = opts || {};
  var evalCount = Number(opts.evalCount || 0);
  var size = opts.size || 'small';
  var stage = evalCount <= 2 ? 'low' : (evalCount <= 9 ? 'mid' : 'high');
  var table = {
    small:  { low: '5,000〜10,000円',  mid: '10,000〜20,000円', high: '15,000〜25,000円' },
    medium: { low: '10,000〜20,000円', mid: '20,000〜40,000円', high: '30,000〜50,000円' },
    large:  { low: '30,000〜50,000円', mid: '40,000〜60,000円', high: '50,000〜80,000円' },
  };
  var hours = { small: '2〜4h', medium: '4〜8h', large: '8〜16h' };
  return { '見積': table[size][stage], '想定実稼働': hours[size] };
}

if (typeof module !== 'undefined') {
  module.exports = { estimate: estimate };
}
```

- [ ] **Step 4: テストを実行して成功を確認**

Run: `cd tools/cw-pipeline/evaluate && node --test`
Expected: PASS（4 tests）

- [ ] **Step 5: コミット**

```bash
git add tools/cw-pipeline/evaluate/estimate.js tools/cw-pipeline/evaluate/estimate.test.js
git commit -m "CW評価ループ: 見積算出 estimate を追加"
```

---

### Task 4: GAS Web App `api.js`（トークン＋doGet(pending)＋initApiToken）→ デプロイ＆curl検証

**Files:**
- Create: `tools/cw-pipeline/api.js`

**Interfaces:**
- Consumes（GASグローバル）: `SHEET_NAME`, `HEADERS`（main.js）、`filterPending`（api-shape.js）
- Produces: `doGet(e)` — `?token` 不一致は `{error:'forbidden'}`。`view=pending`（既定）で `filterPending` の結果をJSONで返す。`initApiToken()` — `CW_API_TOKEN` が無ければ生成し `Logger.log` に出す。

- [ ] **Step 1: `api.js` を作成**

`tools/cw-pipeline/api.js`:

```js
// api.js — GAS Web App: 未判定取得(JSON) / 評価書き戻し(POST) / サマリHTML
// 依存(GASグローバル): SHEET_NAME, HEADERS (main.js), filterPending/findRowByJobId/evalToRowUpdates (api-shape.js)
var API_TOKEN_PROP = 'CW_API_TOKEN';

function initApiToken() {
  var props = PropertiesService.getScriptProperties();
  var t = props.getProperty(API_TOKEN_PROP);
  if (!t) {
    t = Utilities.getUuid().replace(/-/g, '') + Utilities.getUuid().replace(/-/g, '');
    props.setProperty(API_TOKEN_PROP, t);
  }
  Logger.log('CW_API_TOKEN=' + t);
  return t;
}

function _tokenOk(token) {
  var expected = PropertiesService.getScriptProperties().getProperty(API_TOKEN_PROP) || '';
  return expected !== '' && String(token) === expected;
}

function _sheet() {
  return SpreadsheetApp.getActiveSpreadsheet().getSheetByName(SHEET_NAME);
}

function _dataRows(sheet) {
  var last = sheet.getLastRow();
  if (last < 2) return [];
  return sheet.getRange(2, 1, last - 1, HEADERS.length).getValues();
}

function _json(obj) {
  return ContentService.createTextOutput(JSON.stringify(obj)).setMimeType(ContentService.MimeType.JSON);
}

function doGet(e) {
  e = e || {};
  var p = e.parameter || {};
  if (!_tokenOk(p.token)) return _json({ error: 'forbidden' });
  var sheet = _sheet();
  var rows = _dataRows(sheet);
  var view = p.view || 'pending';
  if (view === 'summary') {
    return HtmlService.createHtmlOutput(buildSummaryHtml(rows)).setTitle('CW案件 評価サマリ');
  }
  return _json(filterPending(rows));
}
```

（`doPost` と `buildSummaryHtml` は Task 5/6 で追加。Task 4時点では `doGet` の `view=summary` が未定義関数を呼ぶが、pending検証のみ行うので問題ない。summary検証はTask 6で実施。）

- [ ] **Step 2: GASへ配置**

Apps Scriptエディタ（プロジェクト「CW案件パイプライン」）の `コード.gs` の末尾に、まず api-shape.js（Task 2完成版）と estimate.js（Task 3完成版）の中身を連結し、続けて api.js の中身を貼り付ける。保存。

- [ ] **Step 3: トークン生成**

関数セレクタで `initApiToken` を選び実行 → 実行ログの `CW_API_TOKEN=...` を控える。ローカルの gitignore 済みファイルに保存:

```bash
mkdir -p tools/cw-pipeline/.secrets
printf 'CW_API_TOKEN=%s\n' '<ログのトークン>' > tools/cw-pipeline/.secrets/api.env
echo 'tools/cw-pipeline/.secrets/' >> .gitignore
git add .gitignore && git commit -m "CW評価ループ: APIトークン格納先を gitignore"
```

- [ ] **Step 4: Web Appデプロイ**

Apps Scriptエディタ右上 **デプロイ → 新しいデプロイ → 種類:ウェブアプリ**。「次のユーザーとして実行:自分」「アクセスできるユーザー:全員」。デプロイし **ウェブアプリURL**（`https://script.google.com/macros/s/XXX/exec`）を控え、`.secrets/api.env` に追記:

```bash
printf 'CW_API_URL=%s\n' '<ウェブアプリURL>' >> tools/cw-pipeline/.secrets/api.env
```

- [ ] **Step 5: curlでpending検証**

```bash
set -a; . tools/cw-pipeline/.secrets/api.env; set +a
curl -s -L "$CW_API_URL?token=$CW_API_TOKEN&view=pending" | head -c 400; echo
curl -s -L "$CW_API_URL?token=WRONG&view=pending"; echo
```
Expected: 1本目は未判定案件のJSON配列（現在シートの未判定2件 `13275778`/`13286576` を含む `[{"jobId":"...","url":"...","title":"...","sourceType":"スカウト","row":..}]`）。2本目は `{"error":"forbidden"}`。

- [ ] **Step 6: コミット**

```bash
git add tools/cw-pipeline/api.js
git commit -m "CW評価ループ: Web App api.js(トークン/doGet pending/initApiToken)を追加"
```

---

### Task 5: `doPost`（評価書き戻し）→ curl検証

**Files:**
- Modify: `tools/cw-pipeline/api.js`

**Interfaces:**
- Consumes（GASグローバル）: `findRowByJobId`, `evalToRowUpdates`
- Produces: `doPost(e)` — body `{token, jobId, eval}` をJSONで受け、トークン照合→案件IDで行検索→`evalToRowUpdates(eval)` の各セルを書き込み。`{ok:true,updatedRow}` / `{error:'forbidden'}` / `{error:'not_found',jobId}`。

- [ ] **Step 1: `doPost` を追記**

`api.js` の `doGet` の後に追記:

```js
function doPost(e) {
  var body = {};
  try { body = JSON.parse(e.postData.contents); } catch (err) { body = {}; }
  if (!_tokenOk(body.token)) return _json({ error: 'forbidden' });
  var sheet = _sheet();
  var rows = _dataRows(sheet);
  var idCol = rows.map(function (r) { return String(r[0]); });
  var idx = findRowByJobId(idCol, String(body.jobId));
  if (idx < 0) return _json({ error: 'not_found', jobId: body.jobId });
  var sheetRow = idx + 2;
  evalToRowUpdates(body.eval || {}).forEach(function (u) {
    sheet.getRange(sheetRow, u.col).setValue(u.value);
  });
  return _json({ ok: true, updatedRow: sheetRow });
}
```

- [ ] **Step 2: GASへ反映＆再デプロイ**

`コード.gs` の該当箇所（Task 4で貼った `doGet` の後）に `doPost` を追記して保存。**デプロイ → デプロイを管理 → 編集(鉛筆) → バージョン:新バージョン → デプロイ**（URLは不変）。

- [ ] **Step 3: curlで書き戻し検証（既存の未判定1件をNoGoに）**

```bash
set -a; . tools/cw-pipeline/.secrets/api.env; set +a
curl -s -L -X POST "$CW_API_URL" \
  -H 'Content-Type: application/json' \
  --data '{"token":"'"$CW_API_TOKEN"'","jobId":"13275778","eval":{"判定":"NoGo","判定理由":"検証用のダミー判定","提案数":0,"カテゴリ":"検証"}}'; echo
```
Expected: `{"ok":true,"updatedRow":2}`。スプレッドシートの案件13275778の行で J=NoGo・K=NoGo・L=検証用のダミー判定・M=0・E=検証 が入る。

- [ ] **Step 4: 検証データを戻す（未判定へ）＋not_found確認**

```bash
set -a; . tools/cw-pipeline/.secrets/api.env; set +a
curl -s -L -X POST "$CW_API_URL" -H 'Content-Type: application/json' \
  --data '{"token":"'"$CW_API_TOKEN"'","jobId":"13275778","eval":{"判定":"未判定","判定理由":"","提案数":"","カテゴリ":""}}'; echo
curl -s -L -X POST "$CW_API_URL" -H 'Content-Type: application/json' \
  --data '{"token":"'"$CW_API_TOKEN"'","jobId":"000","eval":{"判定":"Go"}}'; echo
```
Expected: 1本目 `{"ok":true,...}`（Jが再び `未判定` に戻り、pendingに復帰）。2本目 `{"error":"not_found","jobId":"000"}`。

- [ ] **Step 5: コミット**

```bash
git add tools/cw-pipeline/api.js
git commit -m "CW評価ループ: doPost(評価書き戻し)を追加"
```

---

### Task 6: ダッシュボード `buildSummaryHtml` → doGet(summary) 検証

**Files:**
- Modify: `tools/cw-pipeline/api.js`

**Interfaces:**
- Produces: `buildSummaryHtml(rows) → string` — 評価済み（`status` が空でも `未判定` でもない）行を新しい順に、Go/NoGoバッジ・提案数・見積・想定実稼働・判定理由・案件リンクで一覧化。Go案件は提案ドラフト（P列）を `<details>` で展開。`_esc(s)` でHTMLエスケープ。

- [ ] **Step 1: `buildSummaryHtml` と `_esc` を追記**

`api.js` に追記:

```js
function _esc(s) {
  return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
}

function buildSummaryHtml(rows) {
  var evaluated = (rows || []).filter(function (r) {
    var s = String(r[9] || '');
    return s !== '' && s !== '未判定';
  });
  evaluated.reverse();
  var css = '<style>body{font-family:-apple-system,BlinkMacSystemFont,sans-serif;margin:0;padding:16px;background:#f6f7f9;color:#1a1a1a}'
    + 'h1{font-size:18px}.card{background:#fff;border-radius:12px;padding:14px;margin:10px 0;box-shadow:0 1px 3px rgba(0,0,0,.08)}'
    + '.badge{display:inline-block;padding:2px 10px;border-radius:999px;font-size:12px;font-weight:700}'
    + '.b-go{background:#e3f6e9;color:#0a7d32}.b-nogo{background:#fde8ec;color:#b00020}'
    + '.meta{color:#555;font-size:13px;margin:6px 0}.title{font-weight:600;margin:4px 0}'
    + 'a{color:#1558d6}details{margin-top:8px}pre{white-space:pre-wrap;background:#f0f2f5;padding:10px;border-radius:8px;font-size:13px}</style>';
  var items = evaluated.map(function (r) {
    var title = String(r[3] || ''), url = String(r[7] || ''), status = String(r[9] || '');
    var reason = String(r[11] || ''), count = String(r[12] || ''), quote = String(r[13] || '');
    var hours = String(r[14] || ''), draft = String(r[15] || '');
    var isGo = status === 'Go';
    var badge = '<span class="badge ' + (isGo ? 'b-go' : 'b-nogo') + '">' + _esc(status) + '</span>';
    var draftHtml = draft ? '<details><summary>提案ドラフト</summary><pre>' + _esc(draft) + '</pre></details>' : '';
    return '<div class="card">' + badge
      + ' <span class="meta">提案数 ' + _esc(count) + ' / 見積 ' + _esc(quote) + ' / ' + _esc(hours) + '</span>'
      + '<div class="title">' + _esc(title) + '</div>'
      + '<div class="meta">' + _esc(reason) + '</div>'
      + '<div class="meta"><a href="' + _esc(url) + '" target="_blank">案件ページ</a></div>'
      + draftHtml + '</div>';
  }).join('');
  if (!items) items = '<p>評価済みの案件はまだありません。</p>';
  return '<!doctype html><meta name="viewport" content="width=device-width,initial-scale=1">'
    + css + '<h1>CW案件 評価サマリ</h1>' + items;
}
```

- [ ] **Step 2: GASへ反映＆再デプロイ**

`コード.gs` に `buildSummaryHtml`/`_esc` を追記して保存 → **デプロイを管理 → 新バージョン → デプロイ**。

- [ ] **Step 3: ダッシュボード検証（1件だけGoを入れて表示確認）**

```bash
set -a; . tools/cw-pipeline/.secrets/api.env; set +a
curl -s -L -X POST "$CW_API_URL" -H 'Content-Type: application/json' \
  --data '{"token":"'"$CW_API_TOKEN"'","jobId":"13286576","eval":{"判定":"Go","判定理由":"要件具体・提案数少・Instagram運用は定型","提案数":2,"見積":"10,000〜20,000円","想定実稼働":"2〜4h","提案ドラフト":"（型A）お世話になります。ご要望の投稿運用について…（検証用ドラフト）"}}'; echo
```
ブラウザ（claude-in-chrome）で `"$CW_API_URL?view=summary&token=$CW_API_TOKEN"` を開く。
Expected: Goバッジ・提案数2・見積・「提案ドラフト」展開が表示される。スマホ幅でも崩れない。確認後、この行を未判定へ戻す:

```bash
curl -s -L -X POST "$CW_API_URL" -H 'Content-Type: application/json' \
  --data '{"token":"'"$CW_API_TOKEN"'","jobId":"13286576","eval":{"判定":"未判定","判定理由":"","提案数":"","見積":"","想定実稼働":"","提案ドラフト":""}}'; echo
```

- [ ] **Step 4: コミット**

```bash
git add tools/cw-pipeline/api.js
git commit -m "CW評価ループ: サマリHTMLダッシュボード buildSummaryHtml を追加"
```

---

### Task 7: 手順書 `RUNBOOK.md`

**Files:**
- Create: `tools/cw-pipeline/evaluate/RUNBOOK.md`

**Interfaces:**
- Produces: Claudeが日次で辿る手順書。API叩き方・判定基準・ドラフト方針・書き戻しの `eval` スキーマ・ガードレールを含む。

- [ ] **Step 1: `RUNBOOK.md` を作成**

`tools/cw-pipeline/evaluate/RUNBOOK.md`:

````markdown
# CW評価ループ RUNBOOK（Claude日次手順）

ローカルセッションで、稼働中の「案件管理」シートの未判定案件を評価し書き戻す。**送信は絶対にしない。** 詳細な設計は [評価ループ設計書](../../../docs/superpowers/specs/2026-07-07-cw-評価ループ-design.md)。

## 前提
- ローカル実行（crowdworks.jp はクラウド遮断）。Chromeが CW/Google にログイン済み。
- APIの認証情報を読み込む: `set -a; . tools/cw-pipeline/.secrets/api.env; set +a`（`CW_API_URL` / `CW_API_TOKEN`）。
- 現在の受注評価数は **0件**（Phase 0）。見積のステージ判定に使う（`estimate` の `evalCount`）。受注が増えたらこの値を更新。

## 手順
1. **未判定取得**: `curl -s -L "$CW_API_URL?token=$CW_API_TOKEN&view=pending"` → 案件配列。0件なら終了。
2. 各案件について（案件間に数秒空ける・多重アクセスしない）:
   1. claude-in-chrome で `url` を**閲覧のみ**で開く。取得: 提案数 / クライアント評価・発注履歴 / 予算 / 報酬形態 / カテゴリ / 要件全文 / 「継続・長期」有無 / 悪質シグナル。
   2. **Go/No-Go**（[crowdworks.md](../../../docs/crowdworks.md) §2/§4/§7）:
      - No-Go: 提案数30件超 / 時間単価制・コンペ / 月数十時間の常駐（週10h制約違反）/ 未経験優先・カンタン作業・情報商材シグナル(§7) / 直接取引誘導 / クライアント評価4.5未満・仮払いを渋る。
      - Go: 固定報酬・提案数少なめ（理想5件未満）・要件具体・定番4型/AI系に合致。
   3. **見積・想定実稼働**: 案件規模を small/medium/large で見立て、`estimate({evalCount:0, size})`（[estimate.js](estimate.js)）の返り値を使う。
   4. **Go案件のみ提案ドラフト**: 型A（要件明確）/型B（曖昧＝選択肢提示で質問終わり）を [crowdworks.md](../../../docs/crowdworks.md) §4 ＋ [freelance.md](../../../docs/freelance.md) §5 で作成。型Cデモは対象外。
3. **書き戻し**（案件ごと）:
   ```bash
   curl -s -L -X POST "$CW_API_URL" -H 'Content-Type: application/json' \
     --data '{"token":"'"$CW_API_TOKEN"'","jobId":"<ID>","eval":{ ... }}'
   ```
   `eval` キー（提供したものだけ書かれる）: `カテゴリ,予算,報酬形態,判定("Go"|"NoGo"),判定理由,提案数,見積,想定実稼働,提案ドラフト,結果メモ`。判定はJ(ステータス)とK(判定)の両方に入る。
4. **Go案件のドラフト正本**を `docs/proposals/YYYY-MM-DD-<案件>.md`（[_template.md](../../../docs/proposals/_template.md)）に保存。
5. 全案件処理後、`docs/proposals/` と進捗をまとめて日本語コミット＆push。**送信は人間がダッシュボード/シートを見て判断**。

## エラー処理
- 案件が404/募集終了: `eval:{判定:"クローズ",結果メモ:"案件削除/募集終了"}` で書き戻し（Jが変わり再処理されない）。※Jのプルダウンに `クローズ` あり。
- ページ解析に失敗: 書き戻さず `eval:{結果メモ:"解析失敗: <理由>"}` のみ（判定は送らない＝Jは未判定のまま→次回再試行）。
- curl失敗: 数回リトライ。`{"error":"forbidden"}` はトークン不一致→中断して要確認。

## ガードレール
- CW上の応募・送信・メッセージを**一切しない**。書き込み先は自分のシート/リポジトリのみ。
- 人間ペース。1日の未判定は通常一桁。
- 出先確認用ダッシュボード: `"$CW_API_URL?view=summary&token=$CW_API_TOKEN"`。
````

- [ ] **Step 2: コミット**

```bash
git add tools/cw-pipeline/evaluate/RUNBOOK.md
git commit -m "CW評価ループ: 日次手順書 RUNBOOK.md を追加"
```

---

### Task 8: README追記・進捗記録・プッシュ

**Files:**
- Modify: `tools/cw-pipeline/README.md`
- Modify: `docs/progress/2026-07.md`

- [ ] **Step 1: READMEにフェーズ2デプロイ手順を追記**

`tools/cw-pipeline/README.md` の末尾に追記:

````markdown
## フェーズ2: 評価ループのWeb App（出先ダッシュボード）

評価ループ（[evaluate/RUNBOOK.md](evaluate/RUNBOOK.md)）が使うAPI。GASプロジェクトに `api.js`＋`evaluate/api-shape.js`＋`evaluate/estimate.js` の中身を `コード.gs` に追記して配置する。

1. 上記3ファイルの中身を `コード.gs` 末尾に連結して保存。
2. 関数 `initApiToken` を実行 → 実行ログの `CW_API_TOKEN=...` を `tools/cw-pipeline/.secrets/api.env`（gitignore済み）に保存。
3. **デプロイ → 新しいデプロイ → ウェブアプリ**（実行:自分／アクセス:全員）→ ウェブアプリURLを `.secrets/api.env` の `CW_API_URL` に保存。
4. 動作確認: `curl "$CW_API_URL?token=$CW_API_TOKEN&view=pending"` で未判定JSONが返る。
5. 出先確認: スマホで `"$CW_API_URL?view=summary&token=$CW_API_TOKEN"` をブックマーク。

> トークンは秘匿。`.secrets/` はコミットしない。コード改修後は「デプロイを管理→新バージョン」で再デプロイ（URL不変）。
````

- [ ] **Step 2: 進捗ログに追記＆コミット＆プッシュ**

`docs/progress/2026-07.md` の「今月やったこと」に、CW評価ループ（フェーズ2）を設計→TDD実装→Web App配置した旨を1行追記。次アクションの「フェーズ2」を実装済みに更新。

```bash
git add tools/cw-pipeline/README.md docs/progress/2026-07.md
git commit -m "CW評価ループ: Web Appデプロイ手順と進捗を記録"
git push
```

---

## Self-Review（作成者チェック済み）

- **Spec coverage:** 設計書§4(API)→Task4/5/6、§4.3(ダッシュボード)→Task6、§5(列)→Global Constraints＋Task2、§6(手順)→Task7、§7(エラー)→Task7、§8(ガードレール)→Global Constraints＋Task7、§9(テスト/受入)→Task1〜3のnode --test＋Task4/5/6のcurl検証、§10(ファイル構成)→ファイル構成表、§11(スコープ)→対象内のみ。§12の未確定(起動方式)は本MVPでは非実装（手動/手順ベース）で正しく除外。
- **Placeholder scan:** 全ステップに実コード/実コマンド/期待出力を記載。TODOなし。トークン等の秘匿値のみプレースホルダ（`<...>`）で、これは意図的。
- **Type consistency:** 列番号(1-based)が Global Constraints・`evalToRowUpdates`・`doPost`・`buildSummaryHtml` で一貫（E5/F6/G7/J10/K11/L12/M13/N14/O15/P16/R18）。`filterPending` の返り値キー(jobId,url,title,sourceType,row)が Task1テストとRUNBOOKの使用箇所で一致。`estimate` の返り値キー(見積,想定実稼働)が Task3テストとRUNBOOK・`eval`スキーマで一致。`eval` キー名が Task2(`evalToRowUpdates`)・Task5 curl・Task7 RUNBOOK で一致。
````
