# CW案件パイプライン MVP（GAS収集→GSS）実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** CWからのメール（スカウト/おすすめ/保存検索新着）に含まれる案件を、GASが自動でGoogleスプレッドシートに重複なく `未判定` で追記する仕組みを作る。

**Architecture:** リスクの高い解析ロジック（案件URL抽出・タイトル整形・ソース種別判定・重複排除）を副作用のない純粋関数 `parser.js` に集約し、Nodeの標準テストランナー（`node --test`）でユニットテストする。Gmail/スプレッドシートのI/Oは `main.js`（GASへ配置）に置き、Apps Scriptエディタで手動検証する。案件URL `crowdworks.jp/public/jobs/<数字>` の形式が全メール種別で共通なので、パーサは1種類で済む。

**Tech Stack:** Google Apps Script（GmailApp / SpreadsheetApp / PropertiesService / ScriptApp）、素のJavaScript（Node v18+ の `node:test` / `node:assert` でテスト。依存パッケージなし）。

## Global Constraints

- CW案件ページへのアクセス（スクレイピング）はしない。GASはメール本文の範囲だけを扱う。
- 案件URLの抽出は **`crowdworks.jp/public/jobs/(\d+)`** のみを対象とし、`/messages/<id>` 等の他URLは拾わない。
- 重複排除キーは案件ID（列A、文字列として保持）。再実行しても重複行を増やさない（冪等）。
- GSSの案件管理シートのヘッダーと列順は設計書 [2026-07-07-cw-案件パイプライン-design.md](../specs/2026-07-07-cw-案件パイプライン-design.md) の第4章に完全準拠する（列A〜Q）。GASが埋めるのは列A〜Iのみ、列J〜Qは空で用意。
- 新規行のステータス（列I）は既定 `未判定`。
- `parser.js` はGASとNodeの両方で動くよう、末尾にNode用のエクスポートシムを付ける（`if (typeof module !== 'undefined') module.exports = {...}`）。GASでは `module` が未定義のためこの行は無視される。
- リポジトリ上のファイル名は `.js`。Apps Scriptへは同名の `.gs` ファイルとして中身をコピーする（内容は同一のJavaScript）。
- コミットメッセージは日本語で簡潔に。作業ブランチ `claude/crowdworks-profile-setup-2gor9g` にのみコミット＆プッシュする。

---

## ファイル構成

| ファイル | 役割 |
|---|---|
| `tools/cw-pipeline/parser.js` | 純粋関数群（`extractJobIds` / `cleanTitle` / `detectSourceType` / `parseEmail` / `computeNewRows`）。GASへは `parser.gs` として配置 |
| `tools/cw-pipeline/parser.test.js` | `parser.js` のNodeユニットテスト（`node --test`） |
| `tools/cw-pipeline/main.js` | GAS I/O（`ensureSheet` / `getExistingIds` / `collectCwJobs` / `installHourlyTrigger`）。GASへは `main.gs` として配置 |
| `tools/cw-pipeline/README.md` | GSS作成・コード配置・権限承認・トリガー設定の手順書 |

依存の向き: `main.js` は `parser.js` の関数を呼ぶ（GAS上では両 `.gs` がグローバルスコープを共有するため直接呼べる）。テストは `parser.js` のみを対象とする。

---

### Task 1: 案件ID抽出 `extractJobIds`

**Files:**
- Create: `tools/cw-pipeline/parser.js`
- Test: `tools/cw-pipeline/parser.test.js`

**Interfaces:**
- Produces: `extractJobIds(text: string) → string[]` — `text` 中の `crowdworks.jp/public/jobs/<数字>` にマッチする案件IDを**出現順・重複排除**で返す。`/messages/<id>` 等は対象外。該当なしは `[]`。

- [ ] **Step 1: 失敗するテストを書く**

`tools/cw-pipeline/parser.test.js`:

```js
const { test } = require('node:test');
const assert = require('node:assert');
const { extractJobIds } = require('./parser.js');

test('extractJobIds: public/jobs のIDだけを抽出し messages は無視', () => {
  const body = [
    'お世話になります。',
    'https://crowdworks.jp/public/jobs/13286576',
    'https://crowdworks.jp/messages/416582123?ref=um_invitation',
  ].join('\n');
  assert.deepStrictEqual(extractJobIds(body), ['13286576']);
});

test('extractJobIds: 複数案件を出現順・重複排除で返す', () => {
  const body = 'a https://crowdworks.jp/public/jobs/111 b https://crowdworks.jp/public/jobs/222 c https://crowdworks.jp/public/jobs/111';
  assert.deepStrictEqual(extractJobIds(body), ['111', '222']);
});

test('extractJobIds: crowdworks.jp 以外のホストのURLは拾わない', () => {
  const body = 'https://evil-example.com/public/jobs/999 と https://crowdworks.jp/public/jobs/111';
  assert.deepStrictEqual(extractJobIds(body), ['111']);
});

test('extractJobIds: 該当なしは空配列', () => {
  assert.deepStrictEqual(extractJobIds('案件URLなし'), []);
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `cd tools/cw-pipeline && node --test`
Expected: FAIL（`Cannot find module './parser.js'` または `extractJobIds is not a function`）

- [ ] **Step 3: 最小実装**

`tools/cw-pipeline/parser.js`:

```js
function extractJobIds(text) {
  var re = /crowdworks\.jp\/public\/jobs\/(\d+)/g;
  var ids = [];
  var seen = {};
  var m;
  while ((m = re.exec(String(text || ''))) !== null) {
    if (!seen[m[1]]) { seen[m[1]] = true; ids.push(m[1]); }
  }
  return ids;
}

if (typeof module !== 'undefined') {
  module.exports = { extractJobIds: extractJobIds };
}
```

- [ ] **Step 4: テストを実行して成功を確認**

Run: `cd tools/cw-pipeline && node --test`
Expected: PASS（3 tests passing）

- [ ] **Step 5: コミット**

```bash
git add tools/cw-pipeline/parser.js tools/cw-pipeline/parser.test.js
git commit -m "CWパイプライン: 案件ID抽出 extractJobIds を追加"
```

---

### Task 2: タイトル整形 `cleanTitle`

**Files:**
- Modify: `tools/cw-pipeline/parser.js`
- Test: `tools/cw-pipeline/parser.test.js`

**Interfaces:**
- Produces: `cleanTitle(subject: string) → string` — 先頭の `【クラウドワークス】` を除去し、末尾の定型句（`について相談がありました` / `のご案内` / `のお知らせ`）を除去して前後空白をトリムする。案件由来の `【…】`（例:`【Instagram投稿作成案件】`）は残す。

- [ ] **Step 1: 失敗するテストを書く**

`parser.test.js` に追記:

```js
const { cleanTitle } = require('./parser.js');

test('cleanTitle: CW接頭辞を除去し案件側の括弧は残す', () => {
  const s = '【クラウドワークス】【Instagram投稿作成案件】投稿を一緒に盛り上げる仲間を募集します！（※学生不可）について相談がありました';
  assert.strictEqual(cleanTitle(s), '【Instagram投稿作成案件】投稿を一緒に盛り上げる仲間を募集します！（※学生不可）');
});

test('cleanTitle: 接頭辞や定型句が無ければそのまま', () => {
  assert.strictEqual(cleanTitle('スプレッドシート自動化のお仕事'), 'スプレッドシート自動化のお仕事');
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `cd tools/cw-pipeline && node --test`
Expected: FAIL（`cleanTitle is not a function`）

- [ ] **Step 3: 最小実装**

`parser.js` の `if (typeof module...` の前に追記し、エクスポートに追加:

```js
function cleanTitle(subject) {
  var s = String(subject || '').trim();
  s = s.replace(/^【クラウドワークス】\s*/, '');
  s = s.replace(/(について相談がありました|のご案内|のお知らせ)\s*$/, '');
  return s.trim();
}
```

エクスポート行を更新:

```js
if (typeof module !== 'undefined') {
  module.exports = { extractJobIds: extractJobIds, cleanTitle: cleanTitle };
}
```

- [ ] **Step 4: テストを実行して成功を確認**

Run: `cd tools/cw-pipeline && node --test`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add tools/cw-pipeline/parser.js tools/cw-pipeline/parser.test.js
git commit -m "CWパイプライン: タイトル整形 cleanTitle を追加"
```

---

### Task 3: ソース種別判定 `detectSourceType`

**Files:**
- Modify: `tools/cw-pipeline/parser.js`
- Test: `tools/cw-pipeline/parser.test.js`

**Interfaces:**
- Produces: `detectSourceType(subject: string, body: string) → string` — `'スカウト'` / `'保存検索'` / `'おすすめ'` / `'その他'` のいずれかを返す。判定は件名＋本文の連結テキストに対して行う。

**注記:** `保存検索` の検出パターンは実物メール未着のため暫定（設計書 §9）。実物が届いたら文言を確定する。スカウト検出とフォールバックを優先してテストする。

- [ ] **Step 1: 失敗するテストを書く**

`parser.test.js` に追記:

```js
const { detectSourceType } = require('./parser.js');

test('detectSourceType: 相談メールはスカウト', () => {
  assert.strictEqual(detectSourceType('…について相談がありました', '見積もり相談などをさせて頂きたい'), 'スカウト');
});

test('detectSourceType: 判定材料が無ければその他', () => {
  assert.strictEqual(detectSourceType('会員登録完了のご案内', 'はじめてガイド'), 'その他');
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `cd tools/cw-pipeline && node --test`
Expected: FAIL（`detectSourceType is not a function`）

- [ ] **Step 3: 最小実装**

`parser.js` に追記し、エクスポートに追加:

```js
function detectSourceType(subject, body) {
  var text = String(subject || '') + '\n' + String(body || '');
  if (/相談がありました|スカウト|見積もり相談/.test(text)) return 'スカウト';
  if (/新着のお仕事|保存した検索条件|条件に一致|新着のお知らせ/.test(text)) return '保存検索';
  if (/おすすめ|あなたにおすすめ|ピックアップ/.test(text)) return 'おすすめ';
  return 'その他';
}
```

エクスポート行を更新:

```js
if (typeof module !== 'undefined') {
  module.exports = {
    extractJobIds: extractJobIds,
    cleanTitle: cleanTitle,
    detectSourceType: detectSourceType,
  };
}
```

- [ ] **Step 4: テストを実行して成功を確認**

Run: `cd tools/cw-pipeline && node --test`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add tools/cw-pipeline/parser.js tools/cw-pipeline/parser.test.js
git commit -m "CWパイプライン: ソース種別判定 detectSourceType を追加"
```

---

### Task 4: メール1通を案件レコードへ `parseEmail`

**Files:**
- Modify: `tools/cw-pipeline/parser.js`
- Test: `tools/cw-pipeline/parser.test.js`

**Interfaces:**
- Consumes: `extractJobIds`, `cleanTitle`, `detectSourceType`
- Produces: `parseEmail(email: {subject: string, body: string}) → Array<{jobId: string, url: string, title: string, sourceType: string}>` — メール本文中のユニーク案件IDごとに1レコード。`url = 'https://crowdworks.jp/public/jobs/' + jobId`。`title` と `sourceType` はメール共通値。案件IDが無ければ `[]`。

- [ ] **Step 1: 失敗するテストを書く**

`parser.test.js` に追記:

```js
const { parseEmail } = require('./parser.js');

test('parseEmail: スカウトメールから1レコード生成', () => {
  const email = {
    subject: '【クラウドワークス】【Instagram投稿作成案件】投稿を一緒に盛り上げる仲間を募集します！（※学生不可）について相談がありました',
    body: 'PopStar.です。\nhttps://crowdworks.jp/public/jobs/13286576\nhttps://crowdworks.jp/messages/416582123',
  };
  assert.deepStrictEqual(parseEmail(email), [{
    jobId: '13286576',
    url: 'https://crowdworks.jp/public/jobs/13286576',
    title: '【Instagram投稿作成案件】投稿を一緒に盛り上げる仲間を募集します！（※学生不可）',
    sourceType: 'スカウト',
  }]);
});

test('parseEmail: 案件URLの無いメールは空配列', () => {
  assert.deepStrictEqual(parseEmail({ subject: '認証コード', body: 'コードは1234' }), []);
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `cd tools/cw-pipeline && node --test`
Expected: FAIL（`parseEmail is not a function`）

- [ ] **Step 3: 最小実装**

`parser.js` に追記し、エクスポートに追加:

```js
function parseEmail(email) {
  email = email || {};
  var subject = email.subject || '';
  var body = email.body || '';
  var ids = extractJobIds(subject + '\n' + body);
  var title = cleanTitle(subject);
  var sourceType = detectSourceType(subject, body);
  return ids.map(function (id) {
    return {
      jobId: id,
      url: 'https://crowdworks.jp/public/jobs/' + id,
      title: title,
      sourceType: sourceType,
    };
  });
}
```

エクスポートに `parseEmail: parseEmail` を追加。

- [ ] **Step 4: テストを実行して成功を確認**

Run: `cd tools/cw-pipeline && node --test`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add tools/cw-pipeline/parser.js tools/cw-pipeline/parser.test.js
git commit -m "CWパイプライン: メール解析 parseEmail を追加"
```

---

### Task 5: 追記行の算出 `computeNewRows`（重複排除）

**Files:**
- Modify: `tools/cw-pipeline/parser.js`
- Test: `tools/cw-pipeline/parser.test.js`

**Interfaces:**
- Produces: `computeNewRows(records: Array<{jobId,url,title,sourceType}>, existingIds: string[], now: string) → string[][]` — `existingIds`（既にシートにある案件ID）と入力バッチ内の重複を除いた新規案件について、**下表の列A〜Jの順**の行配列を返す。

**確定列（本プラン全体で唯一の正。Task 6のヘッダー・Task 7の追記もこの並びに従う）:**

| 配列index | 列 | 値 |
|---|---|---|
| 0 | A 案件ID | jobId |
| 1 | B 取得日時 | now |
| 2 | C 掲載日 | '' |
| 3 | D タイトル | title |
| 4 | E カテゴリ | '' |
| 5 | F 予算 | '' |
| 6 | G 報酬形態 | '' |
| 7 | H URL | url |
| 8 | I ソース種別 | sourceType |
| 9 | J ステータス | '未判定' |

判定以降（GASは埋めない）は K判定 / L判定理由 / M提案数 / N見積 / O想定実稼働 / P提案ドラフト / Q応募日 / R結果メモ。設計書§4はURL列を欠いていたため、Task 6のStep 1で設計書§4をこの A〜R の並び（H=URLを挿入）に更新する。

- [ ] **Step 1: 失敗するテストを書く**

`parser.test.js` に追記:

```js
const { computeNewRows } = require('./parser.js');

const recs = [
  { jobId: '111', url: 'https://crowdworks.jp/public/jobs/111', title: 'A案件', sourceType: 'スカウト' },
  { jobId: '222', url: 'https://crowdworks.jp/public/jobs/222', title: 'B案件', sourceType: 'その他' },
  { jobId: '111', url: 'https://crowdworks.jp/public/jobs/111', title: 'A案件', sourceType: 'スカウト' },
];

test('computeNewRows: 既存IDとバッチ内重複を除外し列A〜Jの行を返す', () => {
  const rows = computeNewRows(recs, ['222'], '2026-07-07T10:00:00');
  assert.deepStrictEqual(rows, [
    ['111', '2026-07-07T10:00:00', '', 'A案件', '', '', '', 'https://crowdworks.jp/public/jobs/111', 'スカウト', '未判定'],
  ]);
});

test('computeNewRows: 全て既存なら空', () => {
  assert.deepStrictEqual(computeNewRows(recs, ['111', '222'], 'now'), []);
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `cd tools/cw-pipeline && node --test`
Expected: FAIL（`computeNewRows is not a function`）

- [ ] **Step 3: 最小実装**

`parser.js` に追記し、エクスポートに追加:

```js
function computeNewRows(records, existingIds, now) {
  var seen = {};
  (existingIds || []).forEach(function (id) { seen[String(id)] = true; });
  var rows = [];
  (records || []).forEach(function (r) {
    var id = String(r.jobId);
    if (seen[id]) return;
    seen[id] = true;
    rows.push([id, now, '', r.title || '', '', '', '', r.url || '', r.sourceType || '', '未判定']);
  });
  return rows;
}
```

エクスポートに `computeNewRows: computeNewRows` を追加。

- [ ] **Step 4: テストを実行して成功を確認**

Run: `cd tools/cw-pipeline && node --test`
Expected: PASS（全タスクのテストが緑）

- [ ] **Step 5: コミット**

```bash
git add tools/cw-pipeline/parser.js tools/cw-pipeline/parser.test.js
git commit -m "CWパイプライン: 追記行算出 computeNewRows(重複排除)を追加"
```

---

### Task 6: 設計書の列定義を確定（A〜R）＋ GASシート初期化 `ensureSheet`

**Files:**
- Create: `tools/cw-pipeline/main.js`
- （設計書§4は既に A〜R（H=URL挿入済み）に更新済み。Step 1で整合を確認するだけ）

**Interfaces:**
- Produces: `ensureSheet() → Sheet` — アクティブなスプレッドシートに `案件管理` シートが無ければ作成し、1行目に確定ヘッダーを設定、ステータス列（J）にプルダウン（データの入力規則）を設定して、そのSheetを返す。既存なら再設定せず返す。
- 確定ヘッダー（A〜R）: `案件ID, 取得日時, 掲載日, タイトル, カテゴリ, 予算, 報酬形態, URL, ソース種別, ステータス, 判定, 判定理由, 提案数, 見積, 想定実稼働, 提案ドラフト, 応募日, 結果メモ`
- ステータス選択肢: `未判定, Go, NoGo, 応募済, 受注, 失注, 納品, クローズ`

- [ ] **Step 1: 設計書第4章と本プランの列整合を確認**

`docs/superpowers/specs/2026-07-07-cw-案件パイプライン-design.md` の第4章が A〜R（GASが埋める=A〜J、H=URL/I=ソース種別/J=ステータス、以降=K〜R）になっており、本プランTask 5の「確定列」表と一致していることを確認する（既に更新済み。相違があれば設計書側を本プランに合わせる）。

- [ ] **Step 2: `ensureSheet` を実装**

`tools/cw-pipeline/main.js`:

```js
var SHEET_NAME = '案件管理';
var HEADERS = ['案件ID','取得日時','掲載日','タイトル','カテゴリ','予算','報酬形態','URL','ソース種別','ステータス','判定','判定理由','提案数','見積','想定実稼働','提案ドラフト','応募日','結果メモ'];
var STATUS_OPTIONS = ['未判定','Go','NoGo','応募済','受注','失注','納品','クローズ'];
var STATUS_COL = 10; // 列J

function ensureSheet() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sheet = ss.getSheetByName(SHEET_NAME);
  if (!sheet) {
    sheet = ss.insertSheet(SHEET_NAME);
    sheet.getRange(1, 1, 1, HEADERS.length).setValues([HEADERS]);
    sheet.setFrozenRows(1);
    var rule = SpreadsheetApp.newDataValidation().requireValueInList(STATUS_OPTIONS, true).build();
    sheet.getRange(2, STATUS_COL, sheet.getMaxRows() - 1, 1).setDataValidation(rule);
  }
  return sheet;
}
```

- [ ] **Step 3: 手動検証（Apps Scriptエディタ）**

`README.md`（Task 8で作成）の手順で、テスト用スプレッドシートにコードを配置し `ensureSheet` を実行。
Expected: `案件管理` シートが作成され、1行目にA〜Rの18ヘッダーが入り、J列にステータスのプルダウンが表示される。再実行しても壊れない。

- [ ] **Step 4: コミット**

```bash
git add docs/superpowers/specs/2026-07-07-cw-案件パイプライン-design.md tools/cw-pipeline/main.js
git commit -m "CWパイプライン: 列定義をA〜Rに確定し ensureSheet を追加"
```

---

### Task 7: 既存ID取得＋収集本体 `getExistingIds` / `collectCwJobs`

**Files:**
- Modify: `tools/cw-pipeline/main.js`

**Interfaces:**
- Consumes: `ensureSheet`、`parseEmail`、`computeNewRows`（`parser.gs` のグローバル関数）
- Produces:
  - `getExistingIds(sheet) → string[]` — 列A（2行目以降）の案件IDを文字列配列で返す。
  - `collectCwJobs() → void` — Gmailで `from:no-reply@crowdworks.jp` かつ前回チェックポイント以降のメッセージを取得し、各メールを `parseEmail` → `computeNewRows` で新規行を算出し、シート末尾へ追記。処理後にチェックポイント（最新処理メッセージのエポックms）を `PropertiesService` に保存。取得0件でも安全に終了。

- [ ] **Step 1: 実装**

`tools/cw-pipeline/main.js` に追記:

```js
var PROP_LAST_EPOCH = 'CW_PIPELINE_LAST_EPOCH';
var GMAIL_QUERY = 'from:no-reply@crowdworks.jp newer_than:2d';

function getExistingIds(sheet) {
  var last = sheet.getLastRow();
  if (last < 2) return [];
  var values = sheet.getRange(2, 1, last - 1, 1).getValues();
  return values.map(function (r) { return String(r[0]); });
}

function collectCwJobs() {
  var sheet = ensureSheet();
  var props = PropertiesService.getScriptProperties();
  var lastEpoch = Number(props.getProperty(PROP_LAST_EPOCH) || 0);
  var threads = GmailApp.search(GMAIL_QUERY, 0, 50);
  var messages = [];
  threads.forEach(function (t) {
    t.getMessages().forEach(function (m) { messages.push(m); });
  });
  // チェックポイントより新しいメールのみ、古い順に処理
  messages = messages
    .filter(function (m) { return m.getDate().getTime() > lastEpoch; })
    .sort(function (a, b) { return a.getDate().getTime() - b.getDate().getTime(); });

  if (messages.length === 0) { Logger.log('新着CWメールなし'); return; }

  var now = Utilities.formatDate(new Date(), Session.getScriptTimeZone(), 'yyyy-MM-dd HH:mm:ss');
  var maxEpoch = lastEpoch;
  var appended = 0;
  messages.forEach(function (m) {
    var email = { subject: m.getSubject(), body: m.getPlainBody() };
    var records = parseEmail(email);
    var existing = getExistingIds(sheet);
    var rows = computeNewRows(records, existing, now);
    rows.forEach(function (row) { sheet.appendRow(row); appended++; });
    var e = m.getDate().getTime();
    if (e > maxEpoch) maxEpoch = e;
  });
  props.setProperty(PROP_LAST_EPOCH, String(maxEpoch));
  Logger.log('追記件数: ' + appended);
}
```

- [ ] **Step 2: 手動検証（Apps Scriptエディタ）**

既にGmailに届いているCWスカウトメール（`public/jobs/13286576` を含む）がある状態で `collectCwJobs` を実行。
Expected: `案件管理` シートに案件13286576の行が `未判定` で追記され、実行ログに「追記件数: 1」等が出る。

- [ ] **Step 3: 冪等性の確認**

`collectCwJobs` を**もう一度**実行。
Expected: 重複行が増えない（チェックポイントにより再処理されない。仮に再処理されても `computeNewRows` の既存ID除外で行は増えない）。ログは「新着CWメールなし」。

- [ ] **Step 4: 案件URL無しメールの無視を確認**

認証コード等のメールだけの期間で実行、または該当メールを含めて実行。
Expected: 案件URLを含まないメールからは行が追記されない。

- [ ] **Step 5: コミット**

```bash
git add tools/cw-pipeline/main.js
git commit -m "CWパイプライン: 収集本体 collectCwJobs と getExistingIds を追加"
```

---

### Task 8: トリガー設置 `installHourlyTrigger` ＋ 配置手順書 README

**Files:**
- Modify: `tools/cw-pipeline/main.js`
- Create: `tools/cw-pipeline/README.md`

**Interfaces:**
- Produces: `installHourlyTrigger() → void` — `collectCwJobs` の重複したトリガーを除去してから、1時間ごとの時間主導トリガーを1つ設置する。

- [ ] **Step 1: `installHourlyTrigger` を実装**

`tools/cw-pipeline/main.js` に追記:

```js
function installHourlyTrigger() {
  ScriptApp.getProjectTriggers().forEach(function (t) {
    if (t.getHandlerFunction() === 'collectCwJobs') ScriptApp.deleteTrigger(t);
  });
  ScriptApp.newTrigger('collectCwJobs').timeBased().everyHours(1).create();
  Logger.log('1時間ごとのトリガーを設置しました');
}
```

- [ ] **Step 2: 配置手順書を書く**

`tools/cw-pipeline/README.md`:

````markdown
# CW案件パイプライン（GAS収集→スプレッドシート）

CWからのメール（スカウト/おすすめ/保存検索新着）に含まれる案件を、1時間ごとに自動でこのスプレッドシートへ重複なく追記する。

## セットアップ手順

1. Googleドライブで新規スプレッドシートを作成（名前は任意、例「CW案件管理」）。
2. メニュー **拡張機能 → Apps Script** を開く。
3. エディタで以下の2ファイルを作成し、リポジトリの中身をそのまま貼り付ける:
   - `parser.gs` ← `tools/cw-pipeline/parser.js` の内容
   - `main.gs` ← `tools/cw-pipeline/main.js` の内容
4. 関数 `ensureSheet` を実行 → 権限承認ダイアログで許可（Gmail読み取り・スプレッドシート編集）。
   - `案件管理` シートが作成され、ヘッダーとステータスのプルダウンが入る。
5. 関数 `collectCwJobs` を実行 → 既存のCWメールから案件が追記されることを確認。
6. 関数 `installHourlyTrigger` を実行 → 1時間ごとの自動収集を開始。

## 各列の意味

| 列 | 意味 | 誰が埋める |
|---|---|---|
| A〜J | 案件ID/取得日時/掲載日/タイトル/カテゴリ/予算/報酬形態/URL/ソース種別/ステータス | GAS（カテゴリ・予算・報酬形態は空、後でClaudeが補完） |
| K〜R | 判定/判定理由/提案数/見積/想定実稼働/提案ドラフト/応募日/結果メモ | Claude評価（フェーズ2）・手動 |

## 注意
- CW案件ページのスクレイピングはしない（メール解析のみ）。予算・提案数はフェーズ2でClaudeが案件ページから補完する。
- ステータスは手動またはClaudeが更新（未判定→Go/NoGo→応募済→受注/失注→納品）。
````

- [ ] **Step 3: 手動検証（Apps Scriptエディタ）**

`installHourlyTrigger` を実行し、Apps Scriptの「トリガー」画面で `collectCwJobs` の時間主導トリガーが1つだけ存在することを確認。再実行しても2つに増えない。

- [ ] **Step 4: コミット**

```bash
git add tools/cw-pipeline/main.js tools/cw-pipeline/README.md
git commit -m "CWパイプライン: 時間トリガー設置と配置手順READMEを追加"
```

---

### Task 9: 進捗記録の更新

**Files:**
- Modify: `docs/progress/2026-07.md`
- Modify: `docs/cw-profile.md`（ポートフォリオ第1号の候補として追記）

- [ ] **Step 1: 進捗ログに追記**

`docs/progress/2026-07.md` の「今月やったこと」に、CW案件パイプラインMVP（GAS収集→GSS）を実装した旨と、ポートフォリオ第1号を兼ねる旨を追記。ポートフォリオ登録タスクの参照を更新。

- [ ] **Step 2: コミット＆プッシュ**

```bash
git add docs/progress/2026-07.md docs/cw-profile.md
git commit -m "CWパイプライン: MVP実装を進捗ログに記録"
git push
```

---

## Self-Review（作成者チェック済み）

- **Spec coverage:** 設計書の①GAS収集（§5）→ Task 1〜8で網羅。②GSS列（§4）→ Task 6で確定（URL列挿入によりA〜Rへ拡張、本プラン内で一貫）。重複排除（§5.4）→ Task 5＋7。トリガー（§5.1）→ Task 8。非対象=CWスクレイピング（§5.5）→ Global Constraintsに明記。受入基準（§7）→ Task 7の手動検証Step2〜4、Task 8のトリガー確認で対応。フェーズ2/3は非対象で正しく除外。
- **Placeholder scan:** コードはすべて実体を記載。TODOなし。`保存検索` 判定の暫定は設計書§9で根拠明示済み。
- **Type consistency:** `extractJobIds/cleanTitle/detectSourceType/parseEmail/computeNewRows` の引数・戻り値がタスク間で一致。列順は「確定列」表（A〜J＝GAS範囲）を唯一の正として Task 5・6・7・8で統一。`STATUS_COL=10`（列J）と確定列のステータス位置（J）が一致。
