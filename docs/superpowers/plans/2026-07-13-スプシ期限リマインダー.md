# スプシ期限リマインダー 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** スプレッドシートの期限データを、期限のN日前・当日・超過に LINE / Slack へ自動通知する、ノーコード・テンプレ型GASツールを作る。

**Architecture:** 通知先に依存しない純粋関数（期限計算・オフセット選定・冪等な送信状態管理・メッセージ生成・APIペイロード生成）を `core.js` に集約し Node でTDD。GAS結合部（トリガー・UrlFetchApp送信・シート読み書き・カスタムメニュー）は `main.js`。通知はアダプタ式（Slack Incoming Webhook / LINE Messaging API push）。

**Tech Stack:** Google Apps Script（バインドスクリプト）、`GmailApp`不使用・`UrlFetchApp`で送信、テストは Node.js 標準 `node --test` + `node:assert`（既存 `tools/cw-pipeline` と同方式）。

## Global Constraints

- 純粋関数は `core.js` に置き、`if (typeof module !== 'undefined') { module.exports = {...} }` でエクスポート（GASでは `module` 未定義のため無視される。既存 `tools/cw-pipeline/parser.js` と同パターン）。
- テストは `node --test` を使い、`node:test` + `node:assert`。モックは使わず実関数をテスト。
- **LINE Notify は使わない**（2025-03終了）。LINEは Messaging API push（`https://api.line.me/v2/bot/message/push`）。
- Gmailアダプタは v1 では作らない。通知先は `slack` / `line` のみ。
- コミットは日本語メッセージ、末尾に `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 作業ブランチ `claude/crowdworks-profile-setup-2gor9g`。
- 設計書: `docs/superpowers/specs/2026-07-13-スプシ期限リマインダー-design.md`。

---

## File Structure

- `tools/reminder-mailer/core.js` — 純粋関数（テスト対象）: `parseOffsets` / `daysUntilDue` / `renderTemplate` / `parseSentState` / `markSentState` / `selectDue` / `buildSlackPayload` / `buildLinePayload`
- `tools/reminder-mailer/core.test.js` — `node --test`
- `tools/reminder-mailer/main.js` — GAS結合部（テスト対象外）: メニュー・`初回セットアップ`・`setup`・設定/行の読み取り・`onTimeTrigger`・`notify`/`sendViaSlack`/`sendViaLine`・`installDailyTrigger`・`sendTestNotification`・書き戻し
- `tools/reminder-mailer/README.md` — リポジトリ内の開発メモ（配置手順・データモデル）
- `tools/reminder-mailer/手順書.md` — 買い手向け日本語セットアップガイド原本（スクショはTask 9で挿入）

### データモデル（全タスク共通）

**「リマインド一覧」シート列（1始まり）**: A=期限日(Date) / B=件名 / C=本文 / D=有効(TRUE/FALSE) / E=送信状態(自動) / F=最終送信日時(自動) / G=メモ

**config オブジェクト**（「設定」シートから `main.js` が組む。`core.js` はこの形を受け取る）:
```
{
  channel: 'slack' | 'line',
  slackWebhookUrl: string,
  lineToken: string,
  lineUserIds: string[],
  offsets: number[],      // 例 [7,1,0]
  overdueAlert: boolean,
  sendCap: number,
  senderName: string
}
```

**row オブジェクト**（`main.js` が「リマインド一覧」から組み、`selectDue` に渡す）:
```
{ index: number,  // シート行番号（書き戻し用, ヘッダ込みなので2始まり）
  dueDate: Date, subject: string, body: string,
  enabled: boolean, sentState: string }
```

**offsetLabel の規約**: N日前 = `'-N'`（例 `'-7'`）／当日 = `'0'`／超過 = `'超過'`。送信状態セルは空白区切りでラベルを蓄積（例 `'-7 0'`）。

---

## Task 1: リポジトリ雛形 + parseOffsets

**Files:**
- Create: `tools/reminder-mailer/core.js`
- Test: `tools/reminder-mailer/core.test.js`

**Interfaces:**
- Produces: `parseOffsets(str: string): number[]` — カンマ/空白区切り文字列を非負整数の配列に。不正トークンは除外。

- [ ] **Step 1: 失敗するテストを書く**

`tools/reminder-mailer/core.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const { parseOffsets } = require('./core.js');

test('parseOffsets: カンマ区切りを整数配列に', () => {
  assert.deepStrictEqual(parseOffsets('7,1,0'), [7, 1, 0]);
});
test('parseOffsets: 空白混じり・不正トークンを除外', () => {
  assert.deepStrictEqual(parseOffsets(' 7 , x, 1 '), [7, 1]);
});
test('parseOffsets: 空入力は空配列', () => {
  assert.deepStrictEqual(parseOffsets(''), []);
  assert.deepStrictEqual(parseOffsets(null), []);
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: FAIL（`Cannot find module './core.js'` または `parseOffsets is not a function`）

- [ ] **Step 3: 最小実装**

`tools/reminder-mailer/core.js`:
```js
function parseOffsets(str) {
  return String(str == null ? '' : str)
    .split(/[,\s]+/)
    .map(function (s) { return s.trim(); })
    .filter(function (s) { return /^\d+$/.test(s); })
    .map(Number);
}

if (typeof module !== 'undefined') {
  module.exports = { parseOffsets: parseOffsets };
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: PASS（3 tests）

- [ ] **Step 5: コミット**

```bash
cd /Users/shinodzukakazuki/Documents/Claude/general
git add tools/reminder-mailer/core.js tools/reminder-mailer/core.test.js
git commit -m "リマインダー: core雛形とparseOffsetsを追加

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: daysUntilDue

**Files:**
- Modify: `tools/reminder-mailer/core.js`
- Test: `tools/reminder-mailer/core.test.js`

**Interfaces:**
- Produces: `daysUntilDue(today: Date, dueDate: Date): number` — 期限までの日数（当日=0、超過=負）。時刻を無視し日付だけで計算。

- [ ] **Step 1: 失敗するテストを追記**

`core.test.js` 末尾に追記:
```js
const { daysUntilDue } = require('./core.js');

test('daysUntilDue: 未来はプラス', () => {
  assert.strictEqual(daysUntilDue(new Date(2026, 6, 13), new Date(2026, 6, 20)), 7);
});
test('daysUntilDue: 当日は0（時刻差を無視）', () => {
  assert.strictEqual(daysUntilDue(new Date(2026, 6, 13, 23, 0), new Date(2026, 6, 13, 1, 0)), 0);
});
test('daysUntilDue: 超過はマイナス、月またぎも正しい', () => {
  assert.strictEqual(daysUntilDue(new Date(2026, 7, 2), new Date(2026, 6, 31)), -2);
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: FAIL（`daysUntilDue is not a function`）

- [ ] **Step 3: 実装を追記**

`core.js` の `if (typeof module` の前に追記:
```js
function daysUntilDue(today, dueDate) {
  var MS = 86400000;
  var a = Date.UTC(today.getFullYear(), today.getMonth(), today.getDate());
  var b = Date.UTC(dueDate.getFullYear(), dueDate.getMonth(), dueDate.getDate());
  return Math.round((b - a) / MS);
}
```
そして `module.exports` に `daysUntilDue: daysUntilDue,` を追加。

- [ ] **Step 4: テストが通ることを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: PASS（6 tests）

- [ ] **Step 5: コミット**

```bash
git add tools/reminder-mailer/core.js tools/reminder-mailer/core.test.js
git commit -m "リマインダー: daysUntilDue(日付差)を追加

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: renderTemplate

**Files:**
- Modify: `tools/reminder-mailer/core.js`
- Test: `tools/reminder-mailer/core.test.js`

**Interfaces:**
- Produces: `renderTemplate(text: string, vars: object): string` — `{キー}` を `vars[キー]` で置換。未知のキーはそのまま残す。

- [ ] **Step 1: 失敗するテストを追記**

```js
const { renderTemplate } = require('./core.js');

test('renderTemplate: 日本語キーを差し込む', () => {
  const out = renderTemplate('『{件名}』は残り{残り日数}日（{期限日}）', {
    件名: '請求書A', 残り日数: 3, 期限日: '2026-07-20',
  });
  assert.strictEqual(out, '『請求書A』は残り3日（2026-07-20）');
});
test('renderTemplate: 未知のプレースホルダは残す', () => {
  assert.strictEqual(renderTemplate('{宛先}様', {}), '{宛先}様');
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: FAIL（`renderTemplate is not a function`）

- [ ] **Step 3: 実装を追記**

```js
function renderTemplate(text, vars) {
  vars = vars || {};
  return String(text == null ? '' : text).replace(/\{([^{}]+)\}/g, function (m, key) {
    return Object.prototype.hasOwnProperty.call(vars, key) ? String(vars[key]) : m;
  });
}
```
`module.exports` に `renderTemplate: renderTemplate,` を追加。

- [ ] **Step 4: テストが通ることを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: PASS（8 tests）

- [ ] **Step 5: コミット**

```bash
git add tools/reminder-mailer/core.js tools/reminder-mailer/core.test.js
git commit -m "リマインダー: renderTemplate(プレースホルダ差し込み)を追加

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: parseSentState / markSentState（冪等の要）

**Files:**
- Modify: `tools/reminder-mailer/core.js`
- Test: `tools/reminder-mailer/core.test.js`

**Interfaces:**
- Produces: `parseSentState(cell: string): string[]` — 送信状態セルをラベル配列に。
- Produces: `markSentState(cell: string, label: string): string` — ラベルを追記（既存なら重複させない）。空白区切り文字列を返す。

- [ ] **Step 1: 失敗するテストを追記**

```js
const { parseSentState, markSentState } = require('./core.js');

test('parseSentState: 空白区切りをラベル配列に', () => {
  assert.deepStrictEqual(parseSentState('-7 0'), ['-7', '0']);
  assert.deepStrictEqual(parseSentState(''), []);
});
test('markSentState: 新規ラベルを追記', () => {
  assert.strictEqual(markSentState('-7', '0'), '-7 0');
});
test('markSentState: 既存ラベルは重複させない（冪等）', () => {
  assert.strictEqual(markSentState('-7 0', '-7'), '-7 0');
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: FAIL（`parseSentState is not a function`）

- [ ] **Step 3: 実装を追記**

```js
function parseSentState(cell) {
  return String(cell == null ? '' : cell)
    .split(/[,\s]+/).map(function (s) { return s.trim(); }).filter(Boolean);
}
function markSentState(cell, label) {
  var arr = parseSentState(cell);
  if (arr.indexOf(String(label)) === -1) arr.push(String(label));
  return arr.join(' ');
}
```
`module.exports` に `parseSentState` と `markSentState` を追加。

- [ ] **Step 4: テストが通ることを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: PASS（11 tests）

- [ ] **Step 5: コミット**

```bash
git add tools/reminder-mailer/core.js tools/reminder-mailer/core.test.js
git commit -m "リマインダー: 送信状態の解析/追記(冪等)を追加

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: buildSlackPayload / buildLinePayload

**Files:**
- Modify: `tools/reminder-mailer/core.js`
- Test: `tools/reminder-mailer/core.test.js`

**Interfaces:**
- Produces: `buildSlackPayload(message: string): object` → `{ text }`
- Produces: `buildLinePayload(message: string, userId: string): object` → `{ to, messages: [{type:'text', text}] }`

- [ ] **Step 1: 失敗するテストを追記**

```js
const { buildSlackPayload, buildLinePayload } = require('./core.js');

test('buildSlackPayload: textに本文を入れる', () => {
  assert.deepStrictEqual(buildSlackPayload('期限です'), { text: '期限です' });
});
test('buildLinePayload: to と messages を組む', () => {
  assert.deepStrictEqual(buildLinePayload('期限です', 'U123'), {
    to: 'U123', messages: [{ type: 'text', text: '期限です' }],
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: FAIL（`buildSlackPayload is not a function`）

- [ ] **Step 3: 実装を追記**

```js
function buildSlackPayload(message) {
  return { text: String(message) };
}
function buildLinePayload(message, userId) {
  return { to: String(userId), messages: [{ type: 'text', text: String(message) }] };
}
```
`module.exports` に両関数を追加。

- [ ] **Step 4: テストが通ることを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: PASS（13 tests）

- [ ] **Step 5: コミット**

```bash
git add tools/reminder-mailer/core.js tools/reminder-mailer/core.test.js
git commit -m "リマインダー: Slack/LINEペイロード生成を追加

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: selectDue（送信対象の選定＝コアスケジューラ）

**Files:**
- Modify: `tools/reminder-mailer/core.js`
- Test: `tools/reminder-mailer/core.test.js`

**Interfaces:**
- Consumes: `daysUntilDue`, `parseSentState`（同ファイル）
- Produces: `selectDue(rows: Row[], today: Date, config: Config): Array<{index, subject, body, offsetLabel, daysUntilDue}>` — 有効・有効日付の行のうち、設定オフセットに一致し**かつ未送信**のものだけを選ぶ。超過は `overdueAlert` が真かつ `超過` 未送信のとき1回だけ。1行につき最大1件。

- [ ] **Step 1: 失敗するテストを追記**

```js
const { selectDue } = require('./core.js');

const baseConfig = { offsets: [7, 1, 0], overdueAlert: true };
function row(over) {
  return Object.assign({ index: 2, dueDate: new Date(2026, 6, 20), subject: '件名', body: '本文', enabled: true, sentState: '' }, over);
}

test('selectDue: オフセット一致で発火（7日前→-7）', () => {
  const out = selectDue([row({ dueDate: new Date(2026, 6, 20) })], new Date(2026, 6, 13), baseConfig);
  assert.strictEqual(out.length, 1);
  assert.strictEqual(out[0].offsetLabel, '-7');
});
test('selectDue: 当日は0ラベル', () => {
  const out = selectDue([row({ dueDate: new Date(2026, 6, 13) })], new Date(2026, 6, 13), baseConfig);
  assert.strictEqual(out[0].offsetLabel, '0');
});
test('selectDue: 送信済みオフセットは発火しない', () => {
  const out = selectDue([row({ dueDate: new Date(2026, 6, 20), sentState: '-7' })], new Date(2026, 6, 13), baseConfig);
  assert.deepStrictEqual(out, []);
});
test('selectDue: 超過はoverdueAlert時に一度だけ', () => {
  const on = selectDue([row({ dueDate: new Date(2026, 6, 10) })], new Date(2026, 6, 13), baseConfig);
  assert.strictEqual(on[0].offsetLabel, '超過');
  const already = selectDue([row({ dueDate: new Date(2026, 6, 10), sentState: '超過' })], new Date(2026, 6, 13), baseConfig);
  assert.deepStrictEqual(already, []);
  const off = selectDue([row({ dueDate: new Date(2026, 6, 10) })], new Date(2026, 6, 13), { offsets: [7, 1, 0], overdueAlert: false });
  assert.deepStrictEqual(off, []);
});
test('selectDue: 無効行と不正日付はスキップ', () => {
  const disabled = selectDue([row({ enabled: false })], new Date(2026, 6, 13), baseConfig);
  assert.deepStrictEqual(disabled, []);
  const bad = selectDue([row({ dueDate: new Date('invalid') })], new Date(2026, 6, 13), baseConfig);
  assert.deepStrictEqual(bad, []);
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: FAIL（`selectDue is not a function`）

- [ ] **Step 3: 実装を追記**

```js
function selectDue(rows, today, config) {
  config = config || {};
  var offsets = config.offsets || [];
  var out = [];
  (rows || []).forEach(function (r) {
    if (!r || r.enabled === false) return;
    if (!(r.dueDate instanceof Date) || isNaN(r.dueDate.getTime())) return;
    var d = daysUntilDue(today, r.dueDate);
    var sent = parseSentState(r.sentState);
    var label = null;
    if (d >= 0) {
      if (offsets.indexOf(d) !== -1) label = (d === 0 ? '0' : '-' + d);
    } else if (config.overdueAlert) {
      label = '超過';
    }
    if (label && sent.indexOf(label) === -1) {
      out.push({ index: r.index, subject: r.subject, body: r.body, offsetLabel: label, daysUntilDue: d });
    }
  });
  return out;
}
```
`module.exports` に `selectDue: selectDue,` を追加。

- [ ] **Step 4: テストが通ることを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: PASS（18 tests）

- [ ] **Step 5: コミット**

```bash
git add tools/reminder-mailer/core.js tools/reminder-mailer/core.test.js
git commit -m "リマインダー: selectDue(送信対象選定)を追加

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: main.js（GAS結合部）+ README

GAS結合部は Node 単体テスト対象外（`SpreadsheetApp` 等に依存）。手動＋Task 8の実機で確認する。`core.js` の関数はGASでは同一プロジェクト内のグローバルとして参照される（`require` は書かない）。

**Files:**
- Create: `tools/reminder-mailer/main.js`
- Create: `tools/reminder-mailer/README.md`

**Interfaces:**
- Consumes: `parseOffsets`, `selectDue`, `renderTemplate`, `markSentState`, `buildSlackPayload`, `buildLinePayload`（`core.js` のグローバル関数）

- [ ] **Step 1: main.js を作成**

`tools/reminder-mailer/main.js`:
```js
var DATA_SHEET = 'リマインド一覧';
var CONFIG_SHEET = '設定';
var LOG_SHEET = '送信ログ';

function onOpen() {
  SpreadsheetApp.getUi()
    .createMenu('リマインダー')
    .addItem('初回セットアップ', 'firstRunSetup')
    .addItem('テスト送信', 'sendTestNotification')
    .addItem('今すぐ実行', 'onTimeTrigger')
    .addToUi();
}

function firstRunSetup() {
  setup();
  installDailyTrigger();
  var res = sendTestNotification();
  SpreadsheetApp.getUi().alert('初回セットアップ完了: ' + res);
}

function setup() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  ensureSheet(ss, DATA_SHEET, ['期限日', '件名', '本文', '有効', '送信状態', '最終送信日時', 'メモ']);
  ensureSheet(ss, LOG_SHEET, ['日時', '対象行', 'オフセット', '通知先', '結果']);
  var cfg = ss.getSheetByName(CONFIG_SHEET);
  if (!cfg) {
    cfg = ss.insertSheet(CONFIG_SHEET);
    var rows = [
      ['項目', '値'],
      ['通知先(slack/line)', 'slack'],
      ['Slack Webhook URL', ''],
      ['LINEアクセストークン', ''],
      ['LINE userId(カンマ区切り)', ''],
      ['送信オフセット', '7,1,0'],
      ['超過アラート(ON/OFF)', 'ON'],
      ['1回の送信上限', '50'],
      ['差出人名', ''],
    ];
    cfg.getRange(1, 1, rows.length, 2).setValues(rows);
  }
}

function ensureSheet(ss, name, headers) {
  var sh = ss.getSheetByName(name);
  if (!sh) {
    sh = ss.insertSheet(name);
    sh.getRange(1, 1, 1, headers.length).setValues([headers]);
    sh.setFrozenRows(1);
  }
  return sh;
}

function readConfig() {
  var sh = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(CONFIG_SHEET);
  var map = {};
  var v = sh.getRange(2, 1, sh.getLastRow() - 1, 2).getValues();
  v.forEach(function (r) { map[String(r[0]).trim()] = String(r[1]).trim(); });
  return {
    channel: (map['通知先(slack/line)'] || 'slack').toLowerCase(),
    slackWebhookUrl: map['Slack Webhook URL'] || '',
    lineToken: map['LINEアクセストークン'] || '',
    lineUserIds: (map['LINE userId(カンマ区切り)'] || '').split(',').map(function (s) { return s.trim(); }).filter(Boolean),
    offsets: parseOffsets(map['送信オフセット'] || '7,1,0'),
    overdueAlert: /^on$/i.test(map['超過アラート(ON/OFF)'] || 'ON'),
    sendCap: Number(map['1回の送信上限'] || '50'),
    senderName: map['差出人名'] || '',
  };
}

function readRows() {
  var sh = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(DATA_SHEET);
  var last = sh.getLastRow();
  if (last < 2) return [];
  var vals = sh.getRange(2, 1, last - 1, 7).getValues();
  return vals.map(function (r, i) {
    return {
      index: i + 2, dueDate: r[0], subject: r[1], body: r[2],
      enabled: r[3] === true || String(r[3]).toUpperCase() === 'TRUE',
      sentState: r[4],
    };
  });
}

function onTimeTrigger() {
  var config = readConfig();
  var due = selectDue(readRows(), new Date(), config);
  var sent = 0;
  for (var i = 0; i < due.length && sent < config.sendCap; i++) {
    var item = due[i];
    var vars = {
      '件名': item.subject, '期限日': formatDate_(item.dueDate),
      '残り日数': item.daysUntilDue,
    };
    var msg = (config.senderName ? '[' + config.senderName + '] ' : '') +
      renderTemplate(String(item.body || item.subject), vars);
    try {
      notify(config, msg);
      writeBackSent_(item, config.channel);
      sent++;
    } catch (e) {
      logRow_([new Date(), item.index, item.offsetLabel, config.channel, 'ERROR: ' + e.message]);
    }
  }
  return sent + '件送信';
}

function notify(config, message) {
  if (config.channel === 'line') return sendViaLine(config, message);
  return sendViaSlack(config, message);
}

function sendViaSlack(config, message) {
  if (!config.slackWebhookUrl) throw new Error('Slack Webhook URLが未設定です');
  UrlFetchApp.fetch(config.slackWebhookUrl, {
    method: 'post', contentType: 'application/json',
    payload: JSON.stringify(buildSlackPayload(message)),
  });
}

function sendViaLine(config, message) {
  if (!config.lineToken) throw new Error('LINEアクセストークンが未設定です');
  if (!config.lineUserIds.length) throw new Error('LINE userIdが未設定です');
  config.lineUserIds.forEach(function (uid) {
    UrlFetchApp.fetch('https://api.line.me/v2/bot/message/push', {
      method: 'post', contentType: 'application/json',
      headers: { Authorization: 'Bearer ' + config.lineToken },
      payload: JSON.stringify(buildLinePayload(message, uid)),
    });
  });
}

function writeBackSent_(item, channel) {
  var sh = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(DATA_SHEET);
  var cur = sh.getRange(item.index, 5).getValue();
  sh.getRange(item.index, 5).setValue(markSentState(cur, item.offsetLabel));
  sh.getRange(item.index, 6).setValue(formatDate_(new Date(), true));
  logRow_([new Date(), item.index, item.offsetLabel, channel, 'OK']);
}

function logRow_(row) {
  SpreadsheetApp.getActiveSpreadsheet().getSheetByName(LOG_SHEET).appendRow(row);
}

function formatDate_(d, withTime) {
  if (!(d instanceof Date) || isNaN(d.getTime())) return '';
  var tz = Session.getScriptTimeZone();
  return Utilities.formatDate(d, tz, withTime ? 'yyyy-MM-dd HH:mm' : 'yyyy-MM-dd');
}

function installDailyTrigger() {
  ScriptApp.getProjectTriggers().forEach(function (t) {
    if (t.getHandlerFunction() === 'onTimeTrigger') ScriptApp.deleteTrigger(t);
  });
  ScriptApp.newTrigger('onTimeTrigger').timeBased().everyDays(1).atHour(8).create();
}

function sendTestNotification() {
  var config = readConfig();
  var msg = (config.senderName ? '[' + config.senderName + '] ' : '') +
    'これはテスト通知です（スプシ期限リマインダー）。届いていれば設定OKです。';
  notify(config, msg);
  return '通知先=' + config.channel + ' にテスト送信しました';
}
```

- [ ] **Step 2: README.md を作成**

`tools/reminder-mailer/README.md`:
```md
# スプシ期限リマインダー（開発メモ）

スプレッドシートの期限データを LINE / Slack に自動通知するGASツール。設計書: `../../docs/superpowers/specs/2026-07-13-スプシ期限リマインダー-design.md`。

## 構成
- `core.js` — 純粋関数（`node --test core.test.js` でテスト）
- `main.js` — GAS結合部（テンプレのバインドスクリプトに配置。`core.js`→`main.js` の順に連結 or 2ファイルで貼付）
- `手順書.md` — 買い手向けセットアップガイド原本

## GAS配置
1. テンプレ用スプレッドシートの拡張機能→Apps Script。
2. `core.js` と `main.js` の中身を貼付（1ファイル連結でも2ファイルでも可。GASは全.gsがグローバル共有）。
3. スプレッドシートを再読み込み→メニュー「リマインダー」→「初回セットアップ」。

## データモデル
- リマインド一覧: A期限日/B件名/C本文/D有効/E送信状態/F最終送信日時/Gメモ
- 設定: 通知先/Webhook/LINEトークン/userId/オフセット/超過/上限/差出人名
- offsetLabel: `-N`（N日前）/`0`（当日）/`超過`
```

- [ ] **Step 3: テストが壊れていないことを確認**

Run: `cd tools/reminder-mailer && node --test core.test.js`
Expected: PASS（18 tests・main.js追加で影響なし）

- [ ] **Step 4: コミット**

```bash
git add tools/reminder-mailer/main.js tools/reminder-mailer/README.md
git commit -m "リマインダー: GAS結合部main.jsと開発READMEを追加

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: テンプレ配置と実機エンドツーエンド検証（ブラウザ操作）

コードを実際のGoogleスプレッドシートに載せ、Slack と LINE の両方でテスト通知が届くことを確認する。ここで得た画面キャプチャは Task 9 の手順書に使う。

**Files:** （コード変更なし。成果物はテンプレ用スプレッドシートとスクショ）

- [ ] **Step 1: テンプレ用スプレッドシート作成**
新規スプレッドシート「スプシ期限リマインダー（テンプレ）」を作成。拡張機能→Apps Script を開き、`core.js`＋`main.js` の中身を貼付して保存。

- [ ] **Step 2: 初回セットアップ実行**
スプレッドシートを再読み込み→メニュー「リマインダー」→「初回セットアップ」。権限承認ダイアログを承認。`設定`/`リマインド一覧`/`送信ログ` シートが生成されることを確認。各ステップのスクショを取得。

- [ ] **Step 3: Slackでテスト通知**
Slackで Incoming Webhook URL を発行→`設定`シートの「Slack Webhook URL」に貼付、通知先=`slack`。メニュー「テスト送信」→Slackにテスト通知が届くことを確認。Webhook発行〜到達までのスクショを取得。

- [ ] **Step 4: LINEでテスト通知**
LINE Developersで Messaging APIチャネル作成→アクセストークン発行、Botを友だち追加、userId取得。`設定`に貼付、通知先=`line`。メニュー「テスト送信」→LINEに届くことを確認。各ステップのスクショを取得。

- [ ] **Step 5: 発火・冪等の確認**
`リマインド一覧`に期限日=今日の行を1件入れて「今すぐ実行」→通知が届き、E列送信状態に`0`、F列に日時が入る。もう一度「今すぐ実行」→**二重送信されない**（冪等）ことを確認。`送信ログ`に記録されることを確認。

- [ ] **Step 6: スクショ整理**
取得したスクショを `tools/reminder-mailer/manual-assets/`（新規）に `slack-01.png` … / `line-01.png` … の連番で保存。

- [ ] **Step 7: コミット**
```bash
git add tools/reminder-mailer/manual-assets/
git commit -m "リマインダー: 実機検証で取得した手順スクショを追加

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 9: 買い手向け手順書（スクショ多用・Slack編/LINE編）

設計書§7「実装原則B」に沿い、1ステップ=1スクショの日本語手順書を作る。

**Files:**
- Create: `tools/reminder-mailer/手順書.md`

- [ ] **Step 1: 手順書を作成**
`tools/reminder-mailer/手順書.md` に以下の構成で執筆（`manual-assets/` のスクショを各ステップに埋め込み、赤枠・連番・所要時間目安を付す）:
  1. このツールでできること＋**ゴール画像**（テスト通知が届いた画面）
  2. 事前準備（Googleアカウント／SlackまたはLINEのどちらを使うか選ぶ）
  3. **共通**: テンプレをコピー →「初回セットアップ」を押す（スクショ）
  4. **Slack編**: Webhook URL発行→設定シートに貼る→テスト送信（各スクショ、3ステップ）
  5. **LINE編**: OA/チャネル作成→トークン→userId取得→設定シートに貼る→テスト送信（各スクショ）
  6. リマインド一覧の書き方（期限日・件名・本文・オフセット・本文プレースホルダ）
  7. **つまずきポイントFAQ**: 権限承認の警告が出る／通知が来ない／LINEトークン失効／月200通の壁／二重送信されないか
  8. 各セクション冒頭に所要時間目安

- [ ] **Step 2: 第三者視点セルフレビュー**
手順書を上から読み、コードやトリガー画面をユーザーに触らせていないか、Slackが3ステップ以内に収まっているか、各ステップにスクショがあるかを確認。不足を補う。

- [ ] **Step 3: コミット**
```bash
git add tools/reminder-mailer/手順書.md
git commit -m "リマインダー: スクショ多用の買い手向け手順書(Slack編/LINE編)を追加

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 10: BOOTH出品パッケージ草案

出品は外部公開・課金設定のため**ユーザーが実施**。ここでは公開素材と出品文の草案を用意する。

**Files:**
- Create: `tools/reminder-mailer/booth-listing.md`

- [ ] **Step 1: 出品文・価格・素材リストを草案化**
`tools/reminder-mailer/booth-listing.md` に:
  - 商品名候補（3案）・キャッチコピー
  - 商品説明（機能／対応通知先／**差別化=設定が超簡単・二重送信しない・LINE Notify終了対応**／動作環境）
  - 価格: **¥980〜1,480**（設計書§7の入口ウェッジ。gasoji¥2,980より下）
  - 納品物: 公開テンプレのコピーURL＋手順書PDF
  - 必要素材: サムネ、ゴール画像、手順書PDF化
  - 特商法表記・免責（LINE無料枠/自己責任）

- [ ] **Step 2: 手順書をPDF化してパッケージ確認**
`手順書.md` をPDFに書き出し、テンプレのコピーURL（閲覧者がコピー可の共有設定）と合わせて配布物一式が揃うことを確認。

- [ ] **Step 3: コミット＋ユーザーへ出品ハンドオフ**
```bash
git add tools/reminder-mailer/booth-listing.md
git commit -m "リマインダー: BOOTH出品文・価格・素材の草案を追加

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```
出品操作（アップロード・価格設定・公開）はユーザーに依頼する。

---

## Self-Review

- **Spec coverage**: 3シート構成(Task7/8) / LINE+Slackアダプタ(Task5,7) / 発火モデル・冪等(Task6) / 初回セットアップ・テスト送信(Task7,8) / TDD(Task1-6) / 競合差別化=設定最小化・スクショ手順書(Task8,9) / 3チャネル(Task9,10) / CW間口拡大=別タスクのため本計画対象外(設計§10)。網羅。
- **Placeholder scan**: 各コードステップに実コードを記載、TODOなし。
- **Type consistency**: `config`/`row`/`offsetLabel` の形をFile Structureで固定し、`selectDue`(Task6)・`main.js`(Task7)で一致。`markSentState`/`parseSentState`/`buildSlackPayload`/`buildLinePayload` の名称は全タスクで一致。
